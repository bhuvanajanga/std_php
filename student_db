CREATE DATABASE student_db;
USE student_db;

CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(25),
    phone VARCHAR(10),
    course VARCHAR(50)
    year VARCHAR(5),
    About You VARCHAR(150)
);
